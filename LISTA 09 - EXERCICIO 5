
Considere o seguinte array: [8, -8, 64, -20, 3, 1]. Desenhe o passo a passo da
execução do algoritmo Insertion Sort para ordenar em ordem crescente.
#include <stdio.h>

void imprimir_array(int array[], int n)
{
    for(int k = 0; k < n; k++){
        printf("%d ", array[k]);
    }
    printf("\n");
}

void insertion_sort(int arr[], int n){
    for(int i = 1; i < n; i++){
        int chave = arr[i];
        int j = i - 1;
        
        while(j >= 0 && arr[j] > chave){
            arr[j + 1] = arr[j];
            j--;
        }
        
        arr[j + 1] = chave;

        // Mostra o array após cada iteração
        printf("Iteração %d: ", i);
        imprimir_array(arr, n);
    }
}

int main()
{
    int tamanho = 6;
    int array[] = {8, -8, 64, -20, 3, 1};
    
	
    insertion_sort(array, tamanho);

    return 0;
}
